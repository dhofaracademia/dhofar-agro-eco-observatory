# Observatory CI

The executable workflow is `.github/workflows/quality.yml`. It runs all acceptance scripts and the production build.
Workflow installation requires a credential authorised to write workflows. A local file is not proof that Actions is enabled on the repository.
