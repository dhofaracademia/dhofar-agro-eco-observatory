# Seed Intelligence Phase-4 notes (science lock remediation)

**Status:** `scientist_locked_pending_ea` — scores `unvalidated`. **Not** Environment Authority approval.

**Disclaimer:** Unvalidated expert stub — not an Environment Authority approval, not a field-tested score.

**AR:** مسودة خبير غير مُتحقق منها — ليست موافقة من هيئة البيئة، وليست درجة مختبرة ميدانياً.

## Domains (never mix)
- `fog_escarpment` — ميست / ميتان / جميز only
- `najd_arid` — سدر / سمر / غاف only

Former internal aliases `restoration_mountain` / `agriculture_aou` are **not** ecological domain ids; see `run_meta.domain_aliases_note`.

## Catalog
Exactly 6 species from `docs/phase4_species_scaffold.json`. Terminalia Arabic name is **ميست** (عتم = Olea).

## Timing
- Fog: `late_khareef` | `early_post_khareef`; MPI insufficient or onset T0 fallback → `deferred`
- Najd: `winter_spring_rain` | `irrigated` — never mountain MPI

## Provenance
`local_same_jabal` | `regional_dhofar` | `oman_other` | `unknown` | `blocked_exotic` (default `unknown`)

## Hold
Mountain partner UI Hold. No campaign ha/seed kg/crew days. No app/public Seed UI in this PR.

## Cite
`SCIENCE_LOCKS_v0.4_phase4_species` + Product Spec 1.0.1 §12
